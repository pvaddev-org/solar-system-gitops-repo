apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ecr-refresher-rbac.yml
  - ecr-cronjob.yml
  - aws-sealed-secret.yml
# This patch adds the 'imagePullSecrets' to the base deployment
patches:
  - path: patch-image-pull-secrets.yml